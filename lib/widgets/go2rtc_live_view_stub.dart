import 'package:flutter/material.dart';

class Go2rtcLiveView extends StatelessWidget {
  const Go2rtcLiveView({super.key, required this.url});

  final String url;

  @override
  Widget build(BuildContext context) => const ColoredBox(color: Color(0xFF1C1C1E));
}